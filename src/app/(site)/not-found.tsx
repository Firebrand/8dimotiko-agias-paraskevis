import { NotFoundContent } from "@/components/not-found-content";

/** 404 for site routes — rendered with the full header and footer. */
export default function SiteNotFound() {
  return <NotFoundContent />;
}
